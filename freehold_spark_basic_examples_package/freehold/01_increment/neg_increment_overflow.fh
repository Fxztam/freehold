module Neg_Increment_Overflow

procedure increment(x: Integer)
do
    -- Equivalent to SPARK Increment without a precondition.
    -- Static verifier must reject possible overflow when x == Integer.Last.
    x := x + 1
end increment

end Neg_Increment_Overflow
