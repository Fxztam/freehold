module Pos_Increment_Local_Contextual

procedure main()
let x: Integer := 0
-- Contextual/local proof equivalent: the verifier can see the concrete state.
x := x + 1
x := x + 1
check x == 2
end main

end Pos_Increment_Local_Contextual
