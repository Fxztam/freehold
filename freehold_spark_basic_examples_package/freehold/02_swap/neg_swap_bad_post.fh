module Neg_Swap_Bad_Post

procedure swap_bad(x: Integer, y: Integer)
ensures x == old(y)
ensures y == old(x)
do
    -- Bad swap: old x is overwritten before it is copied to y.
    x := y
    y := x
end swap_bad

end Neg_Swap_Bad_Post
