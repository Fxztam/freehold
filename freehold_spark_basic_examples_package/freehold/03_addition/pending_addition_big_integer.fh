module Pending_Addition_Big_Integer

-- Pending until Freehold has an unbounded integer / Big_Integer library.

function addition(x: BigInteger, y: BigInteger) -> BigInteger
ensures result == x + y
do
    return x + y
end addition

end Pending_Addition_Big_Integer
