-- Purpose: Positive test – module with a plain import (no exposing list).
--          Verifies that a plain import is accepted and qualified access works.
-- Referenced Specification: SPECIFICATION-FH-BASICS.md, Section 4.1 (Plain Import)
-- Expected Result: Parser Success, Verifier Success, Runtime Success

module App.Plain

import Std.IO

procedure main()
is
    call Std.IO.log("plain import accepted")
end main

end App.Plain
