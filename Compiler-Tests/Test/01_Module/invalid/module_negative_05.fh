-- Purpose: Negative test – module keyword is present but the module name is missing entirely.
-- Failure Category: Missing module name / invalid module structure
-- Referenced Specification: SPECIFICATION-FH-BASICS.md, Section 1 (Source File Shape),
--                           Section 2.1 (Qualified Module Names)
-- Expected Result: Parser Failure
--   `module` must be followed by at least one identifier.
--   A bare `module` keyword with no name is a syntax error.

module

procedure main()
is
    check true
end main

end
