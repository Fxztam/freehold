module Domain.Types

type Account is record
    id: Integer
    name: String
    tier: String
end record

function make_account(id: Integer, name: String, tier: String) returns Account
ensures result.id = id, result.name = name, result.tier = tier
is
    return Account { id: id, name: name, tier: tier }
end make_account

end Domain.Types