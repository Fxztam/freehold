module Test.CAS.03

function no_default(d: Integer) returns String
requires d >= 1, d <= 3
is
    case d is
        when 1 => return "eins"
        when 2 => return "zwei"
    end case
end no_default

end Test.CAS.03
