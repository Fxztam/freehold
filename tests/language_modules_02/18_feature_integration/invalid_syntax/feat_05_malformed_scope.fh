module Test.FeatureIntegration.Negative.MalformedScope

async function broken_scope() returns Integer
is
    scope bad_scope do
        join
            let x: Integer = 1
        spawn
            let y: Integer = 2
        result
            return 42
    end scope
end broken_scope

end Test.FeatureIntegration.Negative.MalformedScope