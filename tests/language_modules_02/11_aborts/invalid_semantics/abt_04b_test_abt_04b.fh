module Test.ABT.04b

import Test.Support exposing NotFound, InvalidInput
function wrong_abort(n: Integer) returns Integer
aborts NotFound
is
    if n < 0 then
        abort InvalidInput
    end if
    return n
end wrong_abort

end Test.ABT.04b
