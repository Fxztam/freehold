module Test.ABT.04

import Test.Support exposing NotFound
function undeclared(n: Integer) returns Integer
requires n >= 0
is
    if n = 0 then
        abort NotFound
    end if
    return n
end undeclared

end Test.ABT.04
