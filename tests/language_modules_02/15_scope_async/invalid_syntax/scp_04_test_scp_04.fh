module Test.SCP.04

async procedure no_join()
is
    scope bad do
        spawn
            call Std.IO.logf("ok", )
        result
            call Std.IO.logf("done", )
    end scope
end no_join

end Test.SCP.04
