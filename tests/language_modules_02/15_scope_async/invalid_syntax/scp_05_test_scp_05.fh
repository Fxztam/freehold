module Test.SCP.05

async procedure no_result()
is
    scope bad do
        spawn
            call Std.IO.logf("ok", )
        join
            call Std.IO.logf("joined", )
    end scope
end no_result

end Test.SCP.05
