module InferenceConflict

function concat<T>(a: T, b: T) returns T
requires T is Equatable
is
    return a
end concat

procedure main()
is
    let x: Integer = concat(42, "hello")
end main

end InferenceConflict
