module Concurrency.ChannelInvariantPos

function create_verified_channel() returns Channel<Integer>
is
    let chan: Channel<Integer> = channel<Integer>(8) with invariant for all X in 0 .. 0 => X >= 0
    return chan
end create_verified_channel

end Concurrency.ChannelInvariantPos
