module InvalidGuardTypeNeg

choice Option<T> =
    Some(val: T)
  | None
end Option

procedure test(opt: Option<Integer>)
is
    let val: Integer = 0
    case opt is
        | Some(v) when v =>
            val := v
        | Some(v) =>
            val := 0
        | None =>
            val := 0
    end case
end test

end InvalidGuardTypeNeg