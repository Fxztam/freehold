module GenericInferenceConflict

function max<T>(a: T, b: T) returns T
requires T is Comparable
is
    if a > b then
        return a
    else
        return b
    end if
end max

procedure main()
is
    let x: Double = max(3, 4.0)
end main

end GenericInferenceConflict
