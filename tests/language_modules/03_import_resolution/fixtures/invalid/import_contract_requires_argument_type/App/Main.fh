module App.Main

import Domain.Orders exposing load_order
import Domain.Types exposing Order

function checked_order(flag: Boolean) returns Order
requires load_order(flag).id = 1
is
    return load_order(1)
end checked_order

end App.Main