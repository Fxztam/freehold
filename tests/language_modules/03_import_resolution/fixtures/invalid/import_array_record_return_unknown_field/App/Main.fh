module App.Main

import Domain.Orders exposing load_orders
import Domain.Types exposing Order

function checked_orders(id: Integer) returns Array<Order, 2>
ensures result[0].customer.address.postal_code = 1000
is
    return load_orders(id)
end checked_orders

end App.Main
