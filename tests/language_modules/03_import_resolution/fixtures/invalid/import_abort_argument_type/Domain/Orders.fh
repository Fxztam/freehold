module Domain.Orders

import Domain.Types exposing Order, make_order

error NotFound

function read_order(id: Integer) returns Order
aborts NotFound
is
    return make_order(id)
end read_order

end Domain.Orders