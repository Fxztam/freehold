module CommaRequiresEnsures

function clamp(amount: Integer) returns Integer
requires amount >= 0, amount <= 10
ensures result >= 0, result <= 10
is
    return amount
end clamp

end CommaRequiresEnsures